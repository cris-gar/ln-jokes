<!DOCTYPE html>
<html>
  <head>
    <title>LnJokes</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <nav class="bg-gray-800 p-4" data-controller="menu">
    <div class="container mx-auto flex justify-between items-center">
      <div class="text-white font-bold">TuAplicacion</div>
      <button class="text-white block md:hidden" data-action="click->menu#toggle">
        <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
        </svg>
      </button>
      <div class="hidden md:flex md:items-center md:space-x-4" data-menu-target="menu">
        <% if Current.user %>
          <%= link_to 'Perfil', users_path(Current.user.id), class: "text-white ml-4" %>
          <%= link_to 'Cerrar sesión', session_path(Current.user.id), data: {
            turbo_method: :delete,
          }, class: "text-white ml-4" %>
        <% else %>
          <%= link_to 'login', qr_login_path, data: { turbo_frame: "modal" }, class: "text-white ml-4" %>
        <% end %>
      </div>
    </div>
    <div class="hidden md:hidden flex flex-col space-y-4 mt-4" data-menu-target="menu">
      <% if Current.user %>
        <%= link_to 'Perfil', users_path(Current.user.id), class: "block text-white px-4 py-2" %>
        <%= link_to 'Cerrar sesión', session_path(Current.user.id), data: {
          turbo_method: :delete,
        }, class: "block text-white px-4 py-2" %>
      <% else %>
        <%= link_to 'login', qr_login_path, data: { turbo_frame: "modal" }, class: "block text-white px-4 py-2" %>
      <% end %>
    </div>
  </nav>

    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
      <%= turbo_frame_tag "modal" %>
    </main>
  </body>
</html>
